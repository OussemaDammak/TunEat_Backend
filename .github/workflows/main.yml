name: Keep Render App Awake

on:
  schedule:
    - cron: "*/7 * * * *"  # every 10 minutes
  workflow_dispatch:  # allows manual trigger

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render app
        run: |
          curl -I https://tuneat.onrender.com/ping/ > /dev/null
